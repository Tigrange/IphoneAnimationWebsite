{
  "name": "iphone-animation-site",
  "version": "1.0.0",
  "description": "This will load any static html site",
  "scripts": {
    "start": 'browser-sync start --server --files "css/*.css"'
  },
  "dependencies": {
    dev: browser-sync start --server --files "css/*.css",
    share: browser-sync start --proxy "myproject.dev" --files "css/*.css"
  }
}
